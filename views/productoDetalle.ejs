<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalle producto</title>
  </head>
  <body>
    <nav>
      <a href="/">Dashboard</a> <br />
      <a href="/products/create">Agregar productos al catalogo</a> <br />
    </nav>
    <main>
      <div>
        <img
          width="200"
          height="200"
          class="max-w-full h-auto rounded-full"
          src="/assets/images/<%= product.thumbnail %>"
          alt="user-avatar"
        />
        <h3 class="text-2xl text-white font-semibold italic">
          <%= product.title %>
        </h3>
        <p class="text-2xl text-white font-semibold italic">
          <%= product.price %>
        </p>
      </div>
      <div>
        <ul>
          <li><a href="/products/update/<%= product._id %>">editar</a></li>
          <li>
            <form action="/products/delete/<%= product._id %>" method="post">
              <button style="color: red">ELIMINAR</button>
            </form>
          </li>
        </ul>
      </div>
    </main>
  </body>
</html>
